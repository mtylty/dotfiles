# Ruby aliases
alias be "bundle exec"
alias bi "bundle install"
alias bo "bundle open"

# Console aliases
alias g "git"
alias gd "git diff"
alias gs "git status"
alias l "ls"
alias la "ls -la"
alias ll "ls -l"

# Use hub for git
alias git "hub"
